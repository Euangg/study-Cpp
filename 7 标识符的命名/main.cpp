#include<iostream>
using namespace std;

//标识符——变量、常量、函数、结构体、类等的命名
//1.只能使用字母、数字、下划线
//2.名称的第一个字符不能是数字
//3.区分大小写————C++严格区分大小写，不只是标识符，其他地方也不能随便更改大小写
//4.不能用关键字
//5.以下划线和大写字母大头的名称是合法的，但编译器可能会用到，如果使用了，会导致不确定的后果
//5.名称长度没有限制，但有些平台可能有长度限制（一般限制不会小于64个字符）




